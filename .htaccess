# # Enable Rewrite Engine
# RewriteEngine On

# # Remove .php extension from URLs (external redirect - 301 permanent)
# RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s/+([^.\s?]*\.php)(\?[^\s]*)?\s [NC]
# RewriteRule ^ /%1? [R=301,L]

# # Remove trailing slash (except for directories)
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)/$ /$1 [R=301,L]

# # Add .php extension internally if file exists
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME}.php -f
# RewriteRule ^(.*)$ $1.php [L]

# # Prevent directory listing
# Options -Indexes

# # Set default index file
# DirectoryIndex index.php index.html

# # Security: Prevent access to sensitive files
# <FilesMatch "^(config\.php|\.htaccess|\.git|router\.php)">
#     Order allow,deny
#     Deny from all
# </FilesMatch>
